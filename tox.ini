[tox]
skipsdist = True
envlist = py38, py39

[testenv]
deps =
    pytest
    pytest-cov
    requests
    jira
    python-dotenv
    blessed
    six
commands =
    pytest --cov=. --cov-report=xml:coverage.xml automatetest.py
passenv = PYTHONPATH
